name: Test Suite

# integration/bd-fixes is where the fork's work happens; without it the matrix
# never runs on the branch that produces the VSIX. Local verification is macOS
# only, and the VSIX is consumed by Linux devcontainers.
on:
  push:
    branches: [ main, develop, integration/bd-fixes ]
  pull_request:
    branches: [ main, develop, integration/bd-fixes ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: [20.x, 22.x]
      fail-fast: false

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v7
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Lint
      run: npm run lint

    - name: Run VS Code extension tests (Linux)
      if: runner.os == 'Linux'
      run: xvfb-run -a npm test

    - name: Run VS Code extension tests (non-Linux)
      if: runner.os != 'Linux'
      run: npm test

    - name: Verify VSIX package builds
      if: matrix.os == 'ubuntu-latest' && matrix.node-version == '22.x'
      run: |
        npx @vscode/vsce package --no-dependencies
        FILECOUNT=$(npx @vscode/vsce ls *.vsix 2>/dev/null | wc -l)
        echo "VSIX contains $FILECOUNT files"
        if [ "$FILECOUNT" -gt 50 ]; then
          echo "ERROR: VSIX has too many files ($FILECOUNT). Expected under 50. Bundling may be broken."
          exit 1
        fi

